document.getElementById("transferable-courses-list").insertAdjacentHTML(
  "beforeend",
  "<%= j render partial: 'transferable_courses/transferable_course_list_item', locals: { transferable_course: @transferable_course } %>"
);

document.getElementById("new-transferable-course-form").reset();
