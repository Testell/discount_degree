let courseList = document.getElementById("courses-list");
courseList.insertAdjacentHTML('beforeend', "<%= j render(partial: 'courses/course_list_item', locals: { course: @course }) %>");

document.getElementById("new-course-form").reset();
